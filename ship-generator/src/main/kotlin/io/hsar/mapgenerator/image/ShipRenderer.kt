package io.hsar.mapgenerator.image

import io.hsar.mapgenerator.model.Ship

class ShipRenderer(
    private val metresPerPixel: Double,
    private val imageBuilder: ImageBuilder
) {

    fun drawShip(ship: Ship) {

    }
}