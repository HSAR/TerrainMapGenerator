package io.hsar.mapgenerator.framework

import io.hsar.mapgenerator.model.Ship

class ShipGenerator(val shipType: ShipType, val width: Double, val height: Double) {

    fun generateShip(): Ship {
        TODO()
    }
}